{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Jwt": {
    "Issuer": "realtime-chat-api",
    "Audience": "realtime-chat-client",
    "ExpiryMinutes": 60
  },
  "_comment": "ConnectionStrings:DefaultConnection, Jwt:Key and Crypto:Key are secrets and must NOT be committed here — set them with 'dotnet user-secrets set' in Development (a Postgres connection string locally, e.g. Host=localhost;Port=5432;Database=...;Username=...;Password=...), and via environment variables (DATABASE_URL, Jwt__Key, Crypto__Key) in production."
}
